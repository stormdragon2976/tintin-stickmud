#ALIAS {^alig{n|nm|nme|nmen|nment}$}
{
    #echo {%s} {You are ${GMCP_CHAR_STATUS[align]}.}
}
{9}

#ALIAS {^ar{e|ea}$}
{
    #echo {%s} {You are in ${GMCP_ROOM_INFO[area]}.}
}
{9}

#ALIAS {^charswitch$}
{
    #gts {#delay {300} {login}};
    quit;
    #showme {Login will execute in 5 minutes.}
}
{9}

#ALIAS {^tnl$}
{
    #echo {%s} {${GMCP_CHAR_VITALS[nl]} to next level}
}
{9}

#ALIAS {^whoami$}
{
    NOTIFY You are ${GMCP_CHAR_NAME[fullname]}.
}
{9}




